# Docker Images

This directory contains build contexts for GitHub runner and IaC utility images.

- `github-runners/` — runner image definitions.
- `iac-tools/` — tool-focused images (Terraform, Pulumi, Ansible, toolbox).
- `shared/` — shared resources across image builds.
